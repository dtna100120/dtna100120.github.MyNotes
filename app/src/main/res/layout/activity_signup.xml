<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.SignupActivity">

    <TextView
        android:id="@+id/tvNewUser"
        android:text="NEW USER"
        android:textStyle="bold"
        android:textSize="42sp"
        android:textColor="@color/yellow"
        android:textAlignment="center"
        android:layout_marginTop="100dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <TextView
        android:id="@+id/tvNewUser2"
        android:text="Nice to see you here"
        android:textStyle="italic"
        android:textSize="20sp"
        android:textColor="@color/blue"
        android:textAlignment="center"
        app:layout_constraintTop_toBottomOf="@id/tvNewUser"
        app:layout_constraintLeft_toLeftOf="@id/tvNewUser"
        app:layout_constraintRight_toRightOf="@id/tvNewUser"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <EditText
        android:id="@+id/edSEmail"
        app:layout_constraintTop_toBottomOf="@id/tvNewUser2"
        android:inputType="textEmailAddress"
        android:hint="Enter your Email"
        android:layout_marginTop="65dp"
        android:layout_marginLeft="30dp"
        android:layout_marginRight="30dp"
        android:background="@drawable/custom_ed"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <EditText
        android:id="@+id/edSPassword"
        app:layout_constraintTop_toBottomOf="@id/edSEmail"
        android:inputType="textPassword"
        android:hint="Enter your Password"
        android:layout_marginTop="5dp"
        android:layout_marginLeft="30dp"
        android:layout_marginRight="30dp"
        android:background="@drawable/custom_ed"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <Button
        android:id="@+id/btnSignup"
        android:text="Sign up"
        app:layout_constraintTop_toBottomOf="@id/edSPassword"
        android:layout_marginLeft="30dp"
        android:layout_marginRight="30dp"
        android:layout_marginTop="30dp"
        android:textStyle="bold"
        android:background="@drawable/custom_btn"
        android:layout_width="match_parent"
        android:layout_height="50dp"/>

    <TextView
        android:id="@+id/tvWantToLogin"
        android:text="Want to Login?"
        android:textStyle="bold"
        android:textSize="20sp"
        android:layout_marginTop="30dp"
        android:textColor="@color/black"
        app:layout_constraintTop_toBottomOf="@id/btnSignup"
        app:layout_constraintLeft_toLeftOf="@id/btnSignup"
        app:layout_constraintRight_toRightOf="@id/btnSignup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
</androidx.constraintlayout.widget.ConstraintLayout>